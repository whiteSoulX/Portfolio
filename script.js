function toggleMenu{
    const menu = document.querySelector(.menu-links);
    const menu = document.querySelector(.humburger-icon);
    menu.classList.toggle("open");
    icon.classList.toggle("open");
}